/**
 * This class takes care of <code>daily transactions</code> of ABC company.
 * Transactions both <code>sales</code> and <code>expenses</code> are recorded
 * in a <code>storage</code>.
 * A <code>field</code> would mark each <code>transaction</code> either a <code>sale</code>
 * or an <code>expense</code>.
 * At the end of each day, it will display the total <code>sales</code> and
 * <code>expenses</code>.
 * It will also display the total amount of <code>cash at hand</code>.
 */

public class DailyTransactions {
  
}
